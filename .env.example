# [基础配置]
# 管理员密码
ADMIN_PASSWORD=admin123
# 应用对外访问的完整基础 URL
BASE_URL=http://localhost:8848

# [网易云音乐配置]
# NeteaseCloudMusicApi 的访问地址（如果使用同一 Compose 部署，保持默认即可）
NETEASE_API_URL=http://netease-api:3000
# 网易云 Cookie
NETEASE_COOKIE=

# [Bilibili 配置]
# B站 SESSDATA
BILIBILI_SESSDATA=

# [性能与限制配置]
# 播放队列最大长度
QUEUE_MAX_SIZE=1000
# 历史记录保留数量
QUEUE_HISTORY_SIZE=50
# 导入歌单时的最大歌曲数限制
PLAYLIST_IMPORT_LIMIT=100
# 聊天历史记录保留数量
CHAT_HISTORY_LIMIT=1000
# 本地音乐缓存上限 (例如 512MB, 1GB, 2GB)
CACHE_MAX_SIZE=1GB
